[ENABLE]
alloc(newmem, 1000)
alloc(camAngle, 4)

camAngle:
  dd (float)0.0

newmem:
  push eax
  push ecx

  mov ecx, [esi+0x14]
  test ecx, ecx
  jz done

  // Increase angle
  fld dword ptr [camAngle]
  fadd dword ptr [rotSpeed]
  fst dword ptr [camAngle]

  // If angle > 360, subtract 360
  fld dword ptr [maxAngle]
  fcomip st(1)
  jbe wrap
  jmp apply

wrap:
  fsub dword ptr [maxAngle]
  fst dword ptr [camAngle]

apply:
  // 0x29BC = 135 + angle
  fadd dword ptr [baseBC]
  fstp dword ptr [ecx+0x29BC]

  // 0x29C0 = 1100 + angle
  fld dword ptr [camAngle]
  fadd dword ptr [baseC0]
  fstp dword ptr [ecx+0x29C0]

done:
  pop ecx
  pop eax
  mov [esi+0x168], eax
  jmp 00407C72

rotSpeed:
  dd (float)0.1
maxAngle:
  dd (float)360.0
baseBC:
  dd (float)135.0
baseC0:
  dd (float)1100.0

00407C6C:
  jmp newmem
  nop

[DISABLE]
00407C6C:
db 89 86 68 01 00 00
dealloc(newmem)
dealloc(camAngle)
