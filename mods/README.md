# Hamsterball Mods Catalog

| Mod | Description | Files |
|-----|-------------|-------|
| universal-ref-loader | v3: Loads any ref type into any level via vtable[33] hook. JIT mesh injection from disk, clone-on-return for static-mesh objects, difficulty gate bypass, board slot safety checks | [universal-ref-loader/](universal-ref-loader/) |
| global-lifters | Spawns Up Race lifters (Rotators) on any level with a hotkey | [global-lifters/](global-lifters/) |
| global-drawbridge | Spawns Tower Race drawbridges on any level with a hotkey | [global-drawbridge/](global-drawbridge/) |
| global-trapdoor | Spawns Tower Race trapdoors on any level with a hotkey | [global-trapdoor/](global-trapdoor/) |
| global-neon | Spawns all 6 Neon Race objects on any level + neon lighting mode (dark scene with ball-emitted point light). Toggle via NeonLighting/NeonAmbient symbols | [global-neon/](global-neon/) |
| global-expert | Spawns all 6 Expert Race objects (Bonk/Hammer, Fan, Sawblade, Bridge, Judge, Bell) on any level with a hotkey | [global-expert/](global-expert/) |
| global-bonk | Spawns Bonk the Hammer from Expert Race on any level — standalone, constructor loads its own mesh internally | [global-bonk/](global-bonk/) |
| wall-bumpers | All walls act as pinball bumpers — velocity-reversal detection amplifies wall bounces. F8 toggle, F9 force cycle | [wall_bumpers/](wall_bumpers/) |
| ball-break | Press X to shatter your ball and respawn at nearest checkpoint. Calls the game's own Ball_Shatter function | [ball_break/](ball_break/) |
| half-size-all | Shrinks player ball to half size by inlining Ball_Shrink's physics fields (radius=13.0, physics_scale=2.5). No sound, player index 0 only | [half_size_all/](half_size_all/) |
| raptisoft-live-log | Passive logger — taps Raptisoft's hidden in-memory status tracking system (App+0x208/0x20C/0x210) and writes live updates to live_status.txt. No gameplay changes | [raptisoft_live_log/](raptisoft_live_log/) |
| level-colors | Changes per-level base colors (timer oval, timer text, race selection menu text) from a colors.txt config file. Edit at runtime | [level_colors/](level_colors/) |
| custom-name-strips | Custom per-level RGBA colors for the horizontal strip behind the Race/Arena name at the start of each race. Reads from name_strips.txt, 30 entries (15 races + 15 arenas) | [custom_name_strips/](custom_name_strips/) |
| universal-safespots | Adds SAFESPOT(*) universal checkpoint — always accepted as respawn candidate regardless of active SAFESWITCH filter. Competes on distance with lettered SAFESPOTs | [universal-safespots/](universal-safespots/) |
| 8ball-spawn | Press B to spawn an 8-ball in front of the player's hamster ball. Uses mesh index 9 (8Ball), spawns as physics debris (player_index=-1) | [8ball_spawn/](8ball_spawn/) |
| 8ball-goal-fix | Prevents crash when 8-ball (BadBall) touches N:GOAL or E:ACTION(SCORE) triggers. Adds player_index<0 guard (same pattern Raptisoft used for E:LIMIT) to 3 patch points in DispatchCollisionEvents | [8ball_goal_fix/](8ball_goal_fix/) |
| ball-tint | v2: Tints player 1's ball by writing RGBA floats to the board's player color table (board+0x3AB0), the same entries Board_ctor initializes via Vec3_Init. Reads hex color from ball_tint.txt at runtime | [ball_tint/](ball_tint/) |
| global-objects-mkn | Global BUMPER mod — hooks DispatchCollisionEvents to make N:BUMPER collision objects work on ANY level, not just Beginner/Master/Warm-Up/Arena. Applies velocity boost (4×), sound, speed clamping (5-10/12), and hit animation flags | [global_objects_mkn/](global_objects_mkn/) |
| custom-race-descriptions | Customizes the 15 tournament race description texts on the Tourney Menu. Reads from race_descriptions.txt, auto-generates default config with original descriptions | [custom_race_descriptions/](custom_race_descriptions/) |
| lua-support | Embeds Lua 5.1.5 into bass.dll proxy for scripted scene objects. Objects with `L:` prefix in .meshworld files get per-frame Lua callbacks. Full API: get/set position, rotation, scale, ball pos, score, logging | [lua_support/](lua_support/) |
| power-bounce | When player ball lands on top of a shrunken 8-ball (radius < 70% of player), launches player straight up at 2× jump force (40.0f impulse). Combines 8-ball collision detection + jump impulse hooks | [power_bounce/](power_bounce/) |
| mod_targets | Mod manager DLL — deactivates all mods in /mods/ by default, activates them via __mod_targets.txt config (GLOBAL, LEVEL 1-15, ARENA 1-15). Per-race/arena mod loading/unloading via memory polling | [mod_targets/](mod_targets/) |
| neon_arena_lightfix | Fixes missing yellow diffuse light on Neon Arena. ArenaLevel_Neon_Init forgets to set glow flag (+0xC80=1) on P1 balls and omits phys struct material writes that Scene_SetupLevelDark (Neon Race) includes. Polling thread detects Neon Arena and applies yellow ambient/diffuse/emissive + glow flag | [neon_arena_lightfix/](neon_arena_lightfix/) |
| icon_and_windowname | Custom window title and icon mod. Patches App+0x20 pointer (set at 0x42AEE6) to point to user-configurable name string, and applies custom .ico via WM_SETICON after window creation. Config file: icon_and_windowname.txt | [icon_and_windowname/](icon_and_windowname/) |
| custom_credits | Editable credits screen text. Replaces all 44 hardcoded credit lines (30 text + 14 separators) via PUSH imm32 string pointer redirects in CreditsScreen_ctor (0x4254E0). Auto-generates custom_credits.txt with defaults on first launch | [custom_credits/](custom_credits/) |
| updated_audio_libs | Replaces BASS 2.0 audio library (2003) with BASS 2.4.18.3 (Dec 2025). Proxy DLL translates removed API functions (ChannelSetAttributes→ChannelSetAttribute, MusicPlayEx→ChannelPlay). WASAPI output, 20+ years of bug fixes. Includes bass24.dll | [updated_audio_libs/](updated_audio_libs/) |
| difficulty_settings | Difficulty-based entity replacement. Reads difficulty_settings.txt to map entity spawns to replacements (or NOTHING=skip) per tournament difficulty (Pipsqueak/Normal/Frenzied). Hooks Scene_CreateEntities to modify MeshWorld entity names before any factory runs | [difficulty_settings/](difficulty_settings/) |
| custom_sfx | Replace any of the game's 61 sound effects with custom .ogg or .wav files. Hooks Sound_LoadOggOrWav (0x459660) to redirect file loading. Auto-generates custom_sfx.txt listing all sounds with entity/event triggers, buffer counts, and board offsets | [custom_sfx/](custom_sfx/) |
| global_bg_texture | Clouds background overlay — adds clouds.png sprite to any level via Sprite_RenderQuad hook in Graphics_RenderScene. Per-level enable/disable via clouds_bg.txt. RE finding: Sky Race's board+0x4374 sprite is dead code; actual clouds come from CLOUDSCAPE mesh in Level9.MESHWORLD | [global_bg_texture/](global_bg_texture/) |
| ignore_cache | Forces game to always parse .MESHWORLD/.MESH text files, ignoring existing .cached binary files. Also prevents generating new .cached files. 3 byte patches, no DLL. For level modders who swap files frequently | [ignore_cache/](ignore_cache/) |
| custom_entities | Add custom entity types with CE:/E:/N: prefix. CE: entities are references — the level MESHWORLD provides pos/rot/scale, mesh geometry loads from `CustomEntities/<name>.MESHWORLD`, behavior from `CustomEntities/<name>.dll`. Non-invasive polling thread, no code patches. Includes Rotator.dll (X-axis rotation) + Rotator.MESHWORLD | [custom_entities/](custom_entities/) |
| animated_textures | Cycle between numbered texture variants at a custom framerate. Place .txt config files in Textures/ folder — filename becomes animation prefix, mod scans for prefix_NN.ext files and cycles them via D3D8 SetTexture vtable hook. Supports loop-once, loop-forever, and ping-pong modes | [animated_textures/](animated_textures/) |
| discord_rich_presence | Displays current game activity on your Discord profile via raw IPC (no SDK dependency). Shows race name, race number, elapsed timer, or arena mode. Auto-reconnects, 15s rate-limited, toggleable via Options menu | [discord_rich_presence/](discord_rich_presence/) |
| camera_set | Manually set camera orbit angle, zoom distance, pitch, and XYZ offset via Options menu sliders. Background thread writes Scene fields + PatchMemory for hardcoded pitch. Toggle on/off | [camera_set/](camera_set/) |
| freecam | Free-flying camera controlled with WASD+QE+arrow keys. Press F7 to toggle on/off. Uses onRenderApply to replace the view matrix each frame | [freecam/](freecam/) |
| custom_events | Custom E: collision events defined from DLL code. v1: E:SOUND<file>name</file> — plays a custom .ogg/.wav from Sounds/ folder when ball touches the collision plane. Works like E:POPOUT (cooldown + dizzy immunity) with user-specified sound. Hooks DispatchCollisionEvents, auto-detects BASS 2.0/2.4 | [custom_events/](custom_events/) |
| ghost_event | E:GHOST(filename) custom collision event — loads a .ghost file (ghost_saver v22 binary format) and plays it as a ghost ball in ANY game mode (Time Trial, Tournament, Party). Patches Level_UpdateAndRender to remove mode checks, creates ghost ball via Ball_ctor + vtable[1], advances playback index per frame via App_FrameUpdate epilogue hook | [ghost_event/](ghost_event/) |
| ghost_triggers | Proximity triggers for the Time Trial ghost ball. Scans S1 ref points prefixed `GT:` in level MESHWORLD data. When the ghost ball enters a trigger sphere, logs the event. Radius from S1 rot_y field. Frame epilogue hook at 0x46C1F1, per-frame distance check, 60-frame cooldown | [ghost_triggers/](ghost_triggers/) |
| level_warp | E:WARP(N) collision plane that instantly loads a different level when touched. Silently ends the current race (no music/popups) and calls App_StartPracticeRace to load the target level. Supports level numbers (1-15) or names (neon, dizzy, etc.). Hooks DispatchCollisionEvents + App_FrameUpdate epilogue | [level_warp/](level_warp/) |
