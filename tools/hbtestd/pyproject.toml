[project]
name = "hbtestd"
version = "0.3.0"
description = "Automated Hamsterball testing daemon with MCP server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.28.0",
    "psutil>=5.9",
    "pillow>=10.0",
]

[project.scripts]
hbtestd = "hbtestd.server:main"
