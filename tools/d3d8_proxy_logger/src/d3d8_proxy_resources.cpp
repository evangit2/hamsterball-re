//// d3d8_proxy_resources.cpp — Resource wrappers (minimal for now)

#include "d3d8_proxy.h"

// Currently empty — the device file handles all the vtable slots.
// Resource wrapper classes (ProxyTexture8, ProxyVB8, etc.) will be
// implemented here when we switch from forwarding to faking.
