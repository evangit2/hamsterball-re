/* Config stub - HS.CFG loader */
#include <stdio.h>
#include <stdbool.h>
#include "core/config.h"
#include "core/game.h"

bool config_load(config_t *config, const char *path) { (void)config; (void)path; return true; }
bool config_save(config_t *config, const char *path) { (void)config; (void)path; return true; }